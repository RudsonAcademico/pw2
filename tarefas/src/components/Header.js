import React from "react";
import "./Header.css";

function Header() {
    return (
        <header className="header">
            <h1>Suas tarefas — vamos nessa?</h1>
        </header>
    );
}

export default Header;
